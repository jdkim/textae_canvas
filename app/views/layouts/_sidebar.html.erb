<h2>History</h2>
<% if @history.present? %>
  <%= render_history(@history, active_uuid: (@ai_annotation&.uuid || params.dig(:ai_annotation, :branch_from_uuid))) %>
<% else %>
  <p class="history-empty">No history yet</p>
<% end %>
