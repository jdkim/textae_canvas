<%
  stimulus_controller = locals[:stimulus_controller]
%>

<div class="model-field">
  <label>LLM model</label>
  <%%= select_tag :model, options_for_select([["Please select API key first", ""]]),
      { required: true, disabled: true,
        data: { "#{stimulus_controller.gsub('-', '_')}_target": "model", action: "change->#{stimulus_controller.gsub('-', '_')}_target#updateSubmitButton" } } %>
</div>
